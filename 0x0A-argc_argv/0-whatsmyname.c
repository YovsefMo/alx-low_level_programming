#include "main.h"
#include <stdio.h>
/**
 * main - prints programs name
 * @argc: number of command line args
 * @argv: array name
 * Return: 0
 */
int main (int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);

	return (0);
}
